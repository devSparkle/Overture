# VS Code Extension

An official VSCode extension is maintained [here](https://github.com/devSparkle/overture-vscode).

With this extension installed, VS Code will become aware of types, methods, and properties within a library retrieved through `Overture:LoadLibrary`. Several autocomplete features are also provided.